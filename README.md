# IPL KPI on AWS (Death-Overs Run Rate)

This repo computes one KPI from IPL ball-by-ball data:

**Death-overs run rate (overs 16–20)** per match, innings, and batting team.

## Setup
```bash
pip install -r requirements.txt